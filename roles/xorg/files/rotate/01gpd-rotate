# -*- sh -*-
# Xsession.d script to start gpd-rotate
#
# This file is sourced by Xsession(5), not executed.

. /etc/gpd/rotate.conf
/usr/local/sbin/gpd-rotate --screen=${SCREEN} --touchscreen=${TOUCHSCREEN} &